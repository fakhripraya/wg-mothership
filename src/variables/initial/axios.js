import { AXIOS_DEFAULT_ERROR_CONTENT } from "../global";

export const AXIOS_INITIAL_VALUE = {
  responseData: null,
  responseStatus: null,
  responseError: false,
  errorContent: AXIOS_DEFAULT_ERROR_CONTENT,
};
