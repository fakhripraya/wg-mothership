import { AXIOS_DEFAULT_ERROR_CONTENT } from "../global";

export const initialValue = {
    responseData: null,
    responseStatus: null,
    responseError: false,
    errorContent: AXIOS_DEFAULT_ERROR_CONTENT
}